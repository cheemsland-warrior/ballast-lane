Look into the ballast-lane api folder and look for the Dtos folder create an interface mirroring each object and place it on the interfaces folder i just created then create a create-user component that uses a formGroup or whatever the reactive way and gather the info from the user interface(the email etc) and for that we will create a userService in the services folder that has the logic to use the http object that sends a call to the api. You can create a baseService class that has an inner property for URL with "https://localhost:7296/" as it's value, then inherit from that in the two other services and create the necessary methods for CRUD.
So for this prompt I expect the models the services and the just one create-user component for now